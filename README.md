# Escolarsis legacy (EBAM 2018)

Rama **`legacy`**: aplicación original Laravel 5.5 + Vue 2 + Webpack Mix.

Para el desarrollo activo, usa la rama **`escolarsis-cloud`**.

```bash
composer install && npm install && npm run dev
php artisan serve
```
